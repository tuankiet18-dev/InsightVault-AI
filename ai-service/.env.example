GEMINI_API_KEY=
GEMINI_EMBEDDING_MODEL=models/gemini-embedding-001
GEMINI_CHAT_MODEL=models/gemini-2.5-flash

# PostgreSQL connection string (pgvector).
# Target boundary: backend owns business-table writes; AI DB access is MVP/internal only.
DATABASE_URL=postgresql://admin:password123@localhost:5432/insightvault
AI_ALLOW_REPORT_PERSISTENCE=false

# MinIO (AI service reads files for processing after backend has authorized the request)
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=password123
MINIO_SECURE=false

# Processing tuning (optional, defaults are set in core/config.py)
# CHUNK_SIZE_TOKENS=800
# CHUNK_OVERLAP_TOKENS=100
# EMBEDDING_BATCH_SIZE=20
# RAG_TOP_K=5
# RAG_SIMILARITY_THRESHOLD=0.5
# GEMINI_MAX_RETRIES=3
# GEMINI_RETRY_DELAY=2.0
